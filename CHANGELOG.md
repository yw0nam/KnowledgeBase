# Changelog

## Maintenance Contract

This file records outer repository changes only. Use it for changes to source code, scripts, docs, skills, templates, config, schemas, CLI behavior, cron wrappers, and workflow contracts.

Do not record local data artefacts here. Changes under the nested `data/` repo belong in `data/log.md` and the relevant handoff/wiki pages.

Use `## Unreleased` for work not yet released. Within it, use these headings when relevant:

- `### Added` for new features, scripts, skills, templates, docs, or workflows.
- `### Changed` for behavior, contract, schedule, schema, or documentation changes.
- `### Fixed` for bug fixes and corrected broken behavior.
- `### Removed` for deleted features, scripts, skills, docs, or workflows.

Keep entries concise and user/operator-facing. Avoid tool traces, lint output, handoff paths, and private data details unless they are necessary to understand the change.

## Unreleased

### Added

- Added the MCP read tools `query_sql` and `get_schema` (`src/kb/mcp/tools_read.py`), registered on the `kb-web`/MCP server alongside the write tools. `query_sql` is **strictly read-only** with three layers of defense: a statement guard (rejects multi-statement input and any body not starting with `SELECT`/`WITH`), a `SET TRANSACTION READ ONLY` Postgres transaction issued as the first statement of the autobegun transaction (the real control — rejects every write, including data-modifying CTEs that pass the prefix guard), and a `fetchmany(limit)` row cap (default 100, `truncated` flag). DB errors (e.g. a write blocked by the read-only transaction) surface as `{"error", "code": "query_error"}` dicts rather than exceptions, so an agent loop continues. `get_schema` introspects the static ORM metadata (no DB connection) and returns every table's columns plus example queries; agents call it before `query_sql`. The 12-write-tool registration smoke test was relaxed from exact-set to subset so it stays correct as tools are added to the shared `mcp` instance. Read-only is verified against real Postgres in `test/test_mcp_read_tools.py` (10 tests: SELECT, real-data read, prefix-guard rejection of INSERT/UPDATE/DELETE/DROP, multi-statement rejection, the writing-CTE transaction-level block with a no-row assertion, row cap, and schema shape).
- Added the `wiki-note` skill (`.claude/skills/wiki-note/`) for **first-party, human-authored** wiki pages — original notes/insights/decisions with no `data/raw` source. Pages are marked `origin: authored` with `sources: []` and may be born `approved`. This formalizes the existing soft allowance (`kb-lint-wiki` already accepts empty `sources`) as a named, intentional path, while keeping `wiki-authoring` strictly for evidence-derived/LLM/cron pages that must cite real sources — no change to the linter or to `wiki-authoring`/`wiki-approval` logic. Supported types: `concept`, `decision`, `question`, `entity` (improvement/checklist stay on `wiki-authoring`). The skill is exposed globally (added to `GLOBAL_SKILLS` in `install-global-skills.sh`, symlinked to `~/.claude/skills/`) and **self-locates the KB root from its own symlink** so it works from any repo with no `cd`/env/config. `handoff-document` gained the same KB-root self-resolution (new "Step 0") so it too works from any repo. New `origin` field documented in `docs/reference/frontmatter.md`; `wiki-note` wired into `docs/README.md` routing/usage tables and cross-referenced from `wiki-authoring`. Tests in `test/test_wiki_note_skill.py` lock the contract (authored page lints clean; KB-root resolver derives the root from the global symlink and honors a `$KB_ROOT` override).
- Added `docs/workflows.md` — an at-a-glance Mermaid map of the three core flows (nightly cron pipeline, raw→wiki review lifecycle, two-repo `commit→PR→merge` sync) with a shared legend (deterministic 🟦 vs LLM-driven 🟪, gates ⬦, human-run 👤). Explicitly an overview, not an execution guide: each diagram box points back to its owning skill under `.claude/skills/`, and the doc closes with a "where the detail lives" map. Linked from `docs/README.md` (directory map + usage table + PatchNote) and `CLAUDE.md`'s Documentation section.
- Added `knowledgebase-initialize/scripts/install-global-skills.sh` and a new Phase 3.5 ("Expose Global Skills") so a fresh-server init also makes repo skills meant for use outside this repo available globally. It **symlinks** `~/.claude/skills/<name>` → the repo's `.claude/skills/<name>` (repo stays source of truth; a manual copy drifts — `handoff-document`'s global copy already had). Idempotent; a pre-existing real copy is backed up to `~/.claude/skills.pre-symlink-backups/<name>` (outside the scanned skills dir so it is not loaded as a duplicate skill), never deleted. `CLAUDE_SKILLS_DIR` / `CLAUDE_SKILLS_BACKUP_DIR` override destinations; the `GLOBAL_SKILLS` array (currently `handoff-document`) controls the set.
- Added the `data-sync` skill (`.claude/skills/data-sync/`) implementing the work-branch → PR → merge-commit sync model for the nested `data/` repo. Ships five scripts: `setup-data-remote.sh` (attach private origin and enforce merge-commit; moved here from `scripts/`), `setup-data-ci.sh` (install CI lint workflow on `master`; must run before work-branch checkout), `setup-data-workbranch.sh` (migrate `data/` onto a `sync/<machine>-<date>-<rand>` branch), `sync-data.sh` (mandatory local lint gate → push → open/update PR; prune merged branches; detect conflicts), and `merge-data-pr.sh` (GitHub Free private-repo merge gate: wait for remote checks, require `lint=pass`, pin the reviewed head SHA, merge-commit). `kb-cron-wrapup.sh` now holds the canonical `data/.git/kb-sync.lock` across its entire session + log-commit + sync sequence, records sync failures durably, and calls `sync-data.sh` outside the AI session to publish a daily PR. `KB_DATA_DIR` resolution wired into `kb-lint-wiki`, `kb-lint-handoff`, and `kb-wiki-index`. `docs/data-sync.md` rewritten for the work-branch PR model; `knowledgebase-initialize` gains optional Phase 2.5 (remote attach), 2.6 (CI install), and 2.7 (work-branch checkout).
- Added `GET /api/pages/{stem}` returning a single wiki page (any `review_status`) in the same shape as `/api/queue` entries (`{stem, rel_path, abs_path, frontmatter, body}`). The Decisions browser uses it to fetch the full markdown body on row click without depending on the pending-only queue endpoint. Stem lookup is iteration-based (no path-join with user input) and returns 404 when no page matches. The serializer is factored into `src/kb/web/_pages.py` and shared with `/api/queue` so both endpoints stay shape-identical.
- Added Phase 2b Task D: Decisions browser frontend. The review console now has a three-tab top nav — `Pending (n) · Decisions · Dashboard` — with the live `(n)` count of `pending_for_approve` pages reflected in the Pending segment. The new Decisions tab is built around an `Approved · Rejected · Dispatched · Unprocessed` sub-tab row, a four-filter chip row (Type / Category / Source / Edited) backed by a shared custom Dropdown primitive (NOT native `<select>`), and a paginated table with mono `stem` + `type` columns and tabular `edited` figures. URL query params mirror tab + filters + selected stem so reload restores the view. Selecting a row opens the new PageInspector push-rail primitive (default 420px, drag-handle 4px col-resize, hairline visual, `localStorage` persists width, overlay below 1100px viewport). PageInspector renders two zones only — frontmatter editor (top) + audit-driven edit history (bottom) — with NO body preview; the header is a one-line mono stem plus an `Open source ↗` link that hands the markdown to the OS via `file://`. FrontmatterEditor offers four constrained controls (`review_status`, `type`, `category` with free input + suggestions from `GET /api/enums/categories`, `tags` via chip strip), shows a leading mono middot when dirty, disables Save and inlines a warning when a `type` change would require a manual rename, sends only changed fields on PATCH (tags is full-replacement), and renders 409 lint failures inline with form state preserved. EditTimeline renders arrow-anchored rows (`{timestamp} {field}: {old} → {new}`) with 3 most-recent visible by default and a "Show all N" expand that caps at 50vh so Save never falls off-screen. Keyboard surface: `⌘S` / `⌘↵` save, `esc` close (confirm-on-dirty), `j` / `k` row nav, leader `g p` / `g d` / `g a` for tab nav (250ms armed), `?` opens a centered KeyboardHelpOverlay on a low-opacity scrim (no blur, opacity-fade only). Phase 1 cleanup: `kanban_dispatches` is no longer rendered in the page frontmatter strip (it lives in the operational DB now), and `POST /send-to-kanban` consumers have been updated to the new `{id, external_task_id, external_board_id, dispatched_at}` response shape. 10 new vitest specs cover the contracts in spec §9.2 (2 PageInspector, 5 FrontmatterEditor, 2 useUrlFilters, 1 EditTimeline); total frontend test count is 18 (8 pre-existing + 10 new).
- Added Phase 2b Task C: Decisions browser backend endpoints. New `src/kb/db/repos/wiki_edit_repo.py` provides function-style `insert_edits` (one row per change, single commit) and `list_edits` (descending with `since` cursor + unfiltered total). `PATCH /api/pages/{stem}/frontmatter` runs the candidate frontmatter through `kb-lint-wiki` against a hardlink-mirror of the corpus (with regenerated `INDEX.md`), atomically rewrites the file via `os.replace`, and inserts one `wiki_edits` row per changed field. Pipeline order is load-bearing; the recovery contract (file written but DB commit failed → 500 with `file_written: true`; client retry computes empty diff → 200 with `edits: []`) is locked in by tests. `type` changes that would require a cross-directory rename are rejected with 409 before any write. New read endpoints: `GET /api/pages/{stem}/edits` (paged audit history), `GET /api/pages/{stem}/timeline` (UNION of edits and dispatch events), `GET /api/decisions` (filterable page listing with `last_edited_at` and `dispatch_summary` joined from the DB), and `GET /api/enums/categories` (distinct category values; open string, not enforced). CORS now allows `PATCH`. Tests in `test/test_wiki_edit_repo.py` (2) and `test/test_route_decisions.py` (8) cover the repo, all four §9.1 PATCH cases, and the new read contracts.
- Added Phase 2a Task B: DB-backed dispatch ledger. New `src/kb/db/repos/dispatch_repo.py` exposes function-style helpers (`create_dispatch` with Idempotency-Key replay, `update_status` with monotonic `occurred_at` and transition-graph enforcement, `cancel_phase_one`, `cancel_phase_two`, `force_cancel`, `list_dispatches`) and three custom exceptions (`DispatchNotFound`, `TransitionViolation`, `StatusOutOfOrder`). New routes under `/api/dispatches`: paginated listing, Bearer-protected status push (`KB_API_TOKEN`), and a two-state cancel endpoint with `?force=true` escape. `POST /api/pages/{stem}/send-to-kanban` no longer writes the page's frontmatter — it inserts a `dispatches` row and returns `{id, external_task_id, external_board_id, dispatched_at}`. New `kb-migrate-kanban-dispatches` console script backfills existing `kanban_dispatches` frontmatter lists into the DB and removes the frontmatter key (idempotent — UNIQUE blocks dupes). `kb.cli.wiki_review._kanban.append_dispatch` is gone along with its four unit tests and the two Phase 1 rollback route tests. App now wires `app.state.engine` / `app.state.session_factory` via the Task A factories.
- Added Phase 2a Task A foundation for the operational state DB: `sqlalchemy>=2.0` and `alembic>=1.13` dependencies, a `src/kb/db/` module exposing `make_engine`, `make_session_factory`, `get_session`, and a `db_path` helper that resolves `<KB_DATA_DIR>/db/state.db`, plus connection-time PRAGMAs (WAL, foreign_keys=ON, busy_timeout=5000ms, synchronous=NORMAL) wired via a SQLAlchemy `Engine` `connect` listener. Initial Alembic migration creates `dispatches` and `wiki_edits` tables with the spec DDL — CHECK constraints on `status`/`field`/`source`, ISO-8601 KST timestamp shape checks, `json_valid` checks on JSON columns, a partial unique index on `dispatches(idempotency_key)`, status- and stem-keyed lookup indexes, and `BEFORE UPDATE`/`BEFORE DELETE` triggers that abort with `'wiki_edits is append-only'`. `alembic/env.py` reads `KB_DATA_DIR` (default `<repo_root>/data`). Tests in `test/test_db_init.py` cover migration round-trip, PRAGMA application, and trigger enforcement. Tasks B/C/D will build dispatch/wiki-edit repos and routes on this foundation.
- Added a README screenshot for the local review console.
- Added the `cron-wrapup` skill and `kb-cron-wrapup.sh` wrapper for nightly KB operational summaries.
- Added optional global digest guidance under `knowledgebase-initialize/reference/optional-global-digest.md`.
- Added Phase 1 backend for Improvement → Kanban Dispatch: `GET /api/kanban/boards` (with a 30s in-memory TTL cache, invalidated on successful dispatch) and `POST /api/pages/{stem}/send-to-kanban` for sending a `pending_for_approve` improvement page to a Hermes kanban board. Helpers live in `src/kb_mcp/cli/wiki_review/_kanban.py` (`list_boards`, `create_card`, `archive_card`, `append_dispatch`); the route layer translates them per the spec's error taxonomy and writes the dispatch entry back onto the page's `kanban_dispatches` frontmatter list. Unit and route tests added under `test/test_kanban_helpers.py` and `test/test_kanban_route.py`.

### Changed

- Expanded `docs/db_informations/state-db-schema-reference.md` into a complete per-table reference (new Appendix B covering all 11 tables — columns, types, nullability, indexes, CHECK enums, append-only triggers, FKs — generated from the live Postgres), enriched the vocabularies (added `wiki_edits.field`/`source`; noted which enums are app-enforced with no DB CHECK), documented the timestamp/timezone gotcha (`raw_sources.captured_at` is UTC while every other `*_at` is KST-ISO CHECK'd), and added raw-by-KST-day and slug-range read recipes. Wired the doc into the DB-reading skills: `memory-report` and `cron-wrapup` now cite it from their read sections (`wiki-approval` already did).
- Migrated the `memory-report` and `cron-wrapup` skills' source-discovery (read path) from scanning the `data/` Markdown export to direct `psql` queries, so reads go to the Postgres source of truth instead of the human-readable export. `memory-report` Step 0 (daily/weekly/monthly) now queries `raw_sources` (raw ingested on the target KST day, ranged over the UTC-stored `captured_at`), `pages` (summary slugs via `left(slug,10) BETWEEN`/`slug LIKE` and `export_path`), and `handoffs` (`task_slug` + `status='ready'`) instead of `find`/`ls`/`grep` over `data/raw`, `data/wiki/summaries`, and `data/handoffs`. `cron-wrapup` now reads its usage/memory pages and run handoffs from `pages`/`handoffs` via `psql` (with `metrics`/`cron_runs` noted as the canonical structured sources when populated); only the per-run cron logs under `data/raw/ops/cron/` remain file reads, as raw operational evidence not held as wiki content. Write paths were already DB-API-based; this closes the remaining markdown-as-source-of-truth residue on the read side. Queries were validated against the live compose Postgres.
- The CI `lint` job now runs static checks only (`black --check` + `ruff check`) instead of `scripts/lint.sh`. `kb-lint` validates DB content and requires a live Postgres (`DATABASE_URL`), which the CI lint job does not provision — so it stays a local/pre-commit gate via `scripts/lint.sh`. CI lint is now non-mutating (the script ran `black`/`ruff --fix`, which mutated files without verifying the result).
- **Postgres is now the sole source of truth; SQLite support is removed.** `db_url()`/`make_engine()` require `DATABASE_URL` (no SQLite fallback, no `db_path`/PRAGMA listener); `alembic/env.py` requires `DATABASE_URL` and drops `render_as_batch`. All Alembic migrations are rewritten Postgres-only (the SQLite branches and `_upgrade_postgresql` indirection are gone); `page_revisions.source` carries a named `ck_page_revisions_source` CHECK so the `'cli'` widening is a plain `ALTER`. Reads now go **directly to Postgres** via `psql` — the web app is write-only — documented in the new `docs/db_informations/state-db-schema-reference.md` with copy-paste recipes; writes stay gated through the API so lint runs. The `wiki-approval`, `knowledgebase-initialize`, and `cron-wrapup` skills had dead `GET /api/queue` / `/api/pages?type=summary` reads replaced with `psql` queries (and init now brings up the compose Postgres + runs migrations). Host tools reach Postgres through a gitignored `.env` (template `.env.example`) sourced by `scripts/cron/_db.sh` and `scripts/lint.sh`. Tests reuse the compose `db` on `localhost:15432`: a session-scoped migrated template DB is cloned per test (`test/conftest.py`) and dropped after; `test_db_init.py` drops the SQLite-PRAGMA test and adapts the append-only-trigger test to Postgres `RAISE EXCEPTION`. `CLAUDE.md`, `docs/db-canonical.md`, `docs/reference/commands.md`, and `docs/README.md` updated. (The pre-existing local SQLite held only trivial scratch data and the live data already sits in Postgres, so no data migration was performed.)
- Expanded the `wiki-note` skill with per-type bundled templates (`concept`, `decision`, `entity`, `checklist`, `improvement` under `reference/templates/`), each pre-set with the first-party frontmatter (`origin: authored`, `review_status: approved`, `sources: []`) and a `## Related` section using the `- [[stem]] — description` link convention. Supported types now include `checklist` and `improvement` (previously deferred to `wiki-authoring`); the generic `note.md` template was removed in favor of the per-type set. SKILL.md gains a type→path→template table (noting nested folders like `concepts/knowledgebase/` are allowed) and a rule that links live in `## Related`, not inline.
- Removed the hardcoded private-repo slug (`yw0nam/PrivateKnowledgeBase`) from the `data-sync` scripts. The private remote is now whatever `data/`'s own `origin` points at, set when the user clones or attaches their private repo at init — `assert_private_origin` derives `$PRIVATE_REPO` (`owner/name`) from the origin URL and only refuses a non-`github.com` host (the privacy guard), and `setup-data-remote.sh` accepts any github SSH/HTTPS URL and derives the slug from it. `knowledgebase-initialize` Phase 2 now asks whether to **clone an existing private `data/` repo** (origin attached by the clone → skip Phase 2.5) before offering the fresh-init path. This lets a fork/other user point `data/` at their own private repo without editing any script.
- Revised the "never push" invariants in `CLAUDE.md`, `.claude/skills/cron-wrapup/SKILL.md`, `.claude/skills/wiki-approval/SKILL.md`, and `.claude/skills/knowledgebase-initialize/SKILL.md` to clarify that AI sessions and cron-wrapup must not push from inside the session, but `data/` may be pushed to a dedicated private remote (see `docs/data-sync.md`). The outer-repo "never push to remote" guarantee is unchanged.
- Renamed the Python package `kb_mcp` → `kb`. The original name had no relationship to Model Context Protocol; the new name matches the `kb-` CLI prefix. Updated: all `from kb_mcp...` imports across `src/` and `test/`, the `packages` and 9 `[project.scripts]` entries in `pyproject.toml`, the CI coverage path in `.github/workflows/ci.yml`, the GitHub issue/PR template area selectors, `scripts/lint.sh` module paths, frontend file-path comments, the active Phase 2 design handoff, and `.claude/skills/{cron-wrapup,handoff-document,memory-report}/SKILL.md` references. CLI command names (`kb-lint-wiki`, `kb-web`, etc.) are unaffected. **After pulling, run `uv sync`** to re-register console scripts; otherwise `which kb-web` may still resolve to the previous wheel and fail to import. Historical entries above this one still reference `kb_mcp` deliberately — they describe what shipped at the time.
- Documented the existing-local-commits migration recipe in `docs/data-sync.md` Appendix E, with a pointer from `knowledgebase-initialize` Phase 2.7. Use it when a machine's `data/` already carries commits ahead of `origin/master`: the plain Phase 2.6/2.7 path would push those commits straight to `master`, so the recipe backs them up, resets `master`, installs CI, then replays the commits onto a work branch. Also noted that `merge-data-pr.sh` requires a modern `gh` — `gh pr checks --watch`, `gh pr view --json headRefOid`, and `gh pr merge --match-head-commit` are unsupported on `gh` 2.4.0.
- Fixed a class of data-sync CI failures where the local pre-push gate passed but remote CI failed. `sync-data.sh`'s `preflight_lint` regenerates `wiki/INDEX.md` and lints the **working tree**, while remote CI lints the **committed tree**; an uncommitted page edit (e.g. a `review_status` flip from a `wiki-approval` human-approval run) that a regenerated INDEX already reflects on disk could be committed as INDEX-only, passing preflight while CI — regenerating INDEX from the still-pending committed page — found the INDEX stale and failed. Two changes close the gap: (1) the dirty-tree check in `sync-data.sh` (previously a warning) is now a **hard gate** — a non-empty `git status --porcelain` refuses the push (in `--dry-run` it reports "would refuse" without exiting), so only a fully-committed, lint-clean tree is ever pushed and preflight becomes a faithful CI proxy; (2) the `cron-wrapup` skill's Data Commit Policy now stages the **entire** pending `data/` tree (`git add -A`) as one atomic checkpoint and requires `git status --porcelain` to be empty after committing, so a regenerated INDEX is never committed ahead of the pages it is derived from. New `test_sync_refuses_dirty_tree` locks in the gate.

### Deviations from spec (2026-05-26 improvement-to-kanban design)

- `hermes kanban create` does not accept a `--metadata` flag in the installed Hermes. Per Appendix A item 2, the `{kb_page_stem, kb_source}` pair is now embedded in the card body as a trailing `<!-- kb-meta: {...} -->` HTML comment instead of being passed as a JSON parameter. Future tooling that wants to reverse-lookup KB pages from a card must parse the body, not a metadata field.
- `hermes kanban create --json` returns the new task id under `id`, not `task_id`. `_kanban.create_card` normalizes this to `task_id` at the boundary so the route, response payload, and frontmatter all match the spec's contract.
- `counts` on a board listing is sparse (Hermes omits zero buckets), not the fixed five-key shape sketched in §7.1. The route passes the dict through verbatim; the frontend should default missing keys to zero where it needs them.
- §6.1 noted that `kb-lint-wiki` "MUST be updated" to allow `kanban_dispatches`. The current linter has no allowed-keys allowlist (only `REQUIRED_FM_FIELDS`), so no lint change was needed; a regression test in `test/test_kanban_route.py` locks this in.

### Changed

- Updated `knowledgebase-initialize` to include `cron wrap-up` as a required KB cron proposal and the morning digest as optional but recommended.
- Clarified that KnowledgeBase cron jobs use portable wrappers with Hermes cron as the tested scheduler backend, while OpenClaw cron, native cron, and systemd timers are compatible but untested here.
- Expanded the `cron-wrapup` summary contract with `Insights` and `Action Items` sections so daily wrap-ups surface user-facing KB signals, not only job status.
- Updated the cron-wrapup template and optional digest prompt to consume the expanded summary contract.
- Clarified that `kb-cron-wrapup` commits only nested `data/` repo outputs after successful lint, while the optional global digest remains read-only and report-only.
- Cron wrappers now write per-run logs to `data/raw/ops/cron/{YYYY}/{MM}/{TARGET}_kb-<job>.log` instead of appending to `.cron/logs/<job>.log`. The cron wrap-up reads each target's per-run files directly and stages them with its data commit. The `kb-cron-wrapup` wrapper itself still logs to `.cron/logs/cron-wrapup.log` to avoid staging an in-flight file during its own commit.
- Aligned the root `CLAUDE.md` cron-wrapup skill summary with the current per-run cron evidence path under `data/raw/ops/cron/`.

### Fixed

- Fixed three small skill inconsistencies surfaced during the DB-canonical audit: `knowledgebase-initialize` said `GLOBAL_SKILLS` was "currently: `handoff-document`" when the script also ships `wiki-note`; `cron-wrapup`'s workflow header said "8 steps" but listed 10; and the monthly summary templates carried a `month:` field absent from the schema prose — documented `month: "YYYY-MM"  # monthly only` in `memory-report` and `wiki-authoring` (the linter requires only `type/created/updated/sources/tags`, so the field was always lint-valid).
- Corrected docs/skills that still described the pre-migration SQLite model after Postgres became the sole source of truth. `README.md` and `docs/architecture.md` no longer name `data/db/state.db` as the canonical store (now Postgres via `DATABASE_URL`; `data/` is a generated Markdown export, writes through the API, reads via `psql`); the README architecture diagram drops the `data/db/state.db` entry. `knowledgebase-initialize` no longer initializes/verifies a `data/db/state.db` file — it brings up the Postgres `db` service and checks reachability with `psql ... SELECT 1`. `docs/data-sync.md` Appendix C is reframed from "target architecture" to the completed migration. Terminology cleanups: `cron-wrapup` no longer says it `git add`s `data/` logs on commit (data/ is not git-synced); `knowledgebase-initialize`'s global-digest reference says "DB-backed/exported" instead of "committed" summaries.
- Made daily-report DB writes idempotent so re-runs and backfills for the same date no longer fail. `POST /api/pages` now upserts: an existing slug is replaced in place with a `change_kind="update"` revision instead of returning 409. `POST /api/metrics` upserts on `(report_date, report_type)` — a new migration dedupes existing rows (keeping the newest) and replaces the non-unique index with a unique one, so the deterministic per-day metrics export maps 1:1 to a single row.
- Allowed `'cli'` as a `page_revisions.source` value (migration rebuilds the table, preserving its append-only triggers and index) and pointed the three daily-report CLIs at it. Previously they wrote `source="kb-<job>-daily-report"`, which violated the source CHECK constraint and broke the page write on the very first run, not just re-runs.
- Fixed `PATCH /api/pages/{slug}`: a partial-update payload whose `frontmatter` omitted `category` or `review_status` no longer nulls those columns — they are only overwritten when the key is present.
- Fixed `kb-lint --strict`: the flag now actually fails on warnings (previously dead code that never triggered).
- Fixed `kb-wiki-promote.sh` recurring empty-log failures by logging start/failure/lock diagnostics, unsetting inherited `VIRTUAL_ENV`, and wrapping `opencode run` in a 540s child timeout so stale runs release the promotion lock before Hermes cron's 600s script limit.
- Fixed `kb-ingest-papers.sh` cron environment setup by adding the Google Workspace API dependencies to the project environment and unsetting any inherited `VIRTUAL_ENV` before `uv run`, preventing `googleapiclient` import failures and stale active-venv warnings.
- Fixed HuggingFace Daily Papers ingestion date gating: the cron now accepts the latest digest when its subject paper date is either today or yesterday in KST, matching HuggingFace's next-morning delivery pattern.
- Fixed HuggingFace Daily Papers raw frontmatter to use ingestion time for `captured_at` so newly captured digest files satisfy the raw immutability lint.

- Fixed Claude Code daily report tool-call and user-prompt counts being inflated ~250x by overlapping `count_over_time([24h])` sliding windows. `_collect_loki` now uses a new `_query_loki_instant` helper to evaluate the 24h aggregation once at the day boundary instead of summing across query_range step points. Affects `tool_breakdown`, `n_toolcalls`, `error_rate`, and `n_turns`; per-tool error rates were already accurate because numerator and denominator inflated equally.

### Removed

- Removed the legacy `data-sync` skill and the git-based two-repo sync model it implemented, now that Postgres is the sole source of truth and the nightly cron no longer syncs `data/` via Git. Deleted `.claude/skills/data-sync/` (SKILL + `setup-data-remote.sh`/`setup-data-ci.sh`/`setup-data-workbranch.sh`/`sync-data.sh`/`merge-data-pr.sh`/`_lib.sh` + `reference/data-lint.yml`), `docs/data-sync.md`, the `pr-based-data-sync` design spec and plan under `docs/superpowers/`, and the two sync-only test modules (`test/test_data_sync_scripts.py`, `test/test_cron_wrapup_sync.py`). De-referenced the skill/doc from `docs/workflows.md` (dropped the "Two-repo sync" section), `docs/README.md` (document map + routing), `CLAUDE.md`, and the `knowledgebase-initialize` / `cron-wrapup` skills. DB durability is now handled via Postgres backup/export (see `docs/db-canonical.md`), not Git. History remains in Git for anyone who needs the old scripts.
