# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local host environment (DB credentials, API token)
.env

# Obsidian
.obsidian/

# Build artifacts (legacy graphify, retained for safety)
graphify-out/
.skeletons/
.kb_state/

# All operational data lives in nested data/ repo (private, never push).
# Match the bare name too — git-worktrees commonly symlink `data` here.
data
data/

# Runtime cron logs and locks
.cron/

# Playwright MCP local artifacts (screenshots, snapshots, console logs)
.playwright-mcp/

# OS
.DS_Store
